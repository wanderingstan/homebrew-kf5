language: ruby
os: osx
osx_image: xcode9
rvm: system

before_install:
  - brew update || brew update
  - export HOMEBREW_DEVELOPER="1"
install:
  - brew tap chigraph/kf5 file://$PWD
script:
  - ./tools/ci.sh
